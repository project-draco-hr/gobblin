{
  this.maximumRate=maximumRate;
  this.timeUnit=timeUnit;
  this.semaphore=new Semaphore(maximumRate);
  this.permitRestockExecutor=new ScheduledThreadPoolExecutor(1,ExecutorsUtils.newThreadFactory(Optional.of(LOGGER),Optional.of("RateBaseThrottler")));
}
