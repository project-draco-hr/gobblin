{
  if (super.validateTableAttributes() && getFullExtractRunTime() == -1)   throw new MissingExtractAttributeException("Full snapshot tables require a timestamp for full drops.  In most cases, this is when the full extract was started. " + "You can set the timestamp using " + Table.class.getName() + ".setFullTrue(long extractFullRunTime)");
  return true;
}
