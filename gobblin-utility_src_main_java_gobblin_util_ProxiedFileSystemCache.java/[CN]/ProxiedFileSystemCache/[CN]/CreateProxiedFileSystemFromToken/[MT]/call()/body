{
  FileSystem fs=ProxiedFileSystemUtils.createProxiedFileSystemUsingToken(this.userNameToProxyAs,userNameToken,this.uri,this.configuration);
  if (this.referenceFS != null) {
    return decorateFilesystemFromReferenceFS(fs,this.referenceFS);
  }
 else {
    return fs;
  }
}
