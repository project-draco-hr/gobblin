{
  try {
    LOG.info("Flushing pending insert.");
    commands.flush(conn);
    LOG.info("Commiting transaction.");
    conn.commit();
  }
 catch (  Exception e) {
    failed=true;
    throw new RuntimeException(e);
  }
}
