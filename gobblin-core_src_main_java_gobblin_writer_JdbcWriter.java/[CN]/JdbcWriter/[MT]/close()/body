{
  try {
    try {
      if (failed && conn != null) {
        conn.rollback();
      }
    }
  finally {
      if (conn != null) {
        conn.close();
      }
    }
  }
 catch (  SQLException e) {
    throw new RuntimeException(e);
  }
}
