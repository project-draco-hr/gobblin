{
  if (schemas.size() != this.converters.size()) {
    throw new RuntimeException("convertRecord should be called only after convertSchema is called");
  }
  Object record=inputRecord;
  for (  Converter converter : this.converters) {
    record=converter.convertRecord(this.schemas.get(converter),record,workUnit);
  }
  return record;
}
