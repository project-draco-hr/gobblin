{
  try {
    Class<? extends Compactor> compactorClass=getCompactorClass();
    Compactor compactor=compactorClass.getDeclaredConstructor(Properties.class,List.class).newInstance(this.properties,Tag.fromMap(AzkabanTags.getAzkabanTags()));
    return compactor;
  }
 catch (  Exception e) {
    throw new RuntimeException("Failed to instantiate compactor",e);
  }
}
