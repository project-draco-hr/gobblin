{
  if (lowProp == null)   return highProp;
  if (highProp == null)   return lowProp;
  for (  String key : lowProp.keySet()) {
    if (!highProp.containsKey(key)) {
      highProp.put(key,lowProp.get(key));
    }
  }
  return highProp;
}
