{
  LOG.info(String.format("Moving %s to %s",src,dst));
  parallelRunner.movePath(src,this.publisherFileSystemByBranches.get(branchId),dst,this.publisherFinalDirOwnerGroupsByBranches.get(branchId));
}
