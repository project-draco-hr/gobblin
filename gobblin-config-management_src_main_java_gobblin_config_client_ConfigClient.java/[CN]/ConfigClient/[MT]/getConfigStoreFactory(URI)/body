{
  ConfigStoreFactory csf=configStoreFactoryMap.get(uri.getScheme());
  if (csf == null) {
    throw new ConfigStoreFactoryDoesNotExistsException("can not find corresponding config store factory for scheme " + uri.getScheme());
  }
  return (ConfigStoreFactory<ConfigStore>)csf;
}
