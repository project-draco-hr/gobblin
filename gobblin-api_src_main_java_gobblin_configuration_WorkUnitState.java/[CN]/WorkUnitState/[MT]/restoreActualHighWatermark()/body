{
  if (contains(WORK_UNIT_STATE_ACTUAL_HIGH_WATER_MARK_BACKUP_KEY)) {
    setProp(ConfigurationKeys.WORK_UNIT_STATE_ACTUAL_HIGH_WATER_MARK_KEY,getProp(WORK_UNIT_STATE_ACTUAL_HIGH_WATER_MARK_BACKUP_KEY));
  }
}
