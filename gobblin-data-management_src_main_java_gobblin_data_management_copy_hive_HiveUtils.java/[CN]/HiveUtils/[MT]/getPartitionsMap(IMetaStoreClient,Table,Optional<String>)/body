{
  return Maps.uniqueIndex(getPartitions(client,table,filter),new Function<Partition,List<String>>(){
    @Override public List<String> apply(    Partition partition){
      return partition.getValues();
    }
  }
);
}
