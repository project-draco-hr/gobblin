{
  super(String.format("Desired target location %s and already registered target location %s do not agree.",desired,existing));
  this.desiredTargetTableLocation=desired;
  this.existingTargetTableLocation=existing;
}
