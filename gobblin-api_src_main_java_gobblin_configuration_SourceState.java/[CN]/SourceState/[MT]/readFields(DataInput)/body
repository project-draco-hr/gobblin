{
  int size=in.readInt();
  for (int i=0; i < size; i++) {
    WorkUnitState state=new WorkUnitState();
    state.readFields(in);
    this.previousWorkUnitStates.add(state);
  }
  super.readFields(in);
}
