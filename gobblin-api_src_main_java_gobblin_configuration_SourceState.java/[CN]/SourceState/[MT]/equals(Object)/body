{
  if (!(object instanceof SourceState)) {
    return false;
  }
  SourceState other=(SourceState)object;
  return super.equals(other) && this.previousSourceState.equals(other.previousSourceState) && this.previousWorkUnitStates.equals(other.previousWorkUnitStates);
}
