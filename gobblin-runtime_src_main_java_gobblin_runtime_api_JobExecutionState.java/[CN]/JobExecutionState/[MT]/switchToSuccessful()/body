{
  this.changeLock.lock();
  try {
    Preconditions.checkState(RunningState.RUNNING == this.runningState,"unexpected state:" + this.runningState);
    doRunningStateChange(RunningState.SUCCESSFUL);
  }
  finally {
    this.changeLock.unlock();
  }
}
