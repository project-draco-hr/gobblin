{
  this.changeLock.lock();
  try {
    Preconditions.checkState(RunningState.PENDING == this.runningState,"unexpected state:" + this.runningState);
    doRunningStateChange(RunningState.RUNNING);
  }
  finally {
    this.changeLock.unlock();
  }
}
