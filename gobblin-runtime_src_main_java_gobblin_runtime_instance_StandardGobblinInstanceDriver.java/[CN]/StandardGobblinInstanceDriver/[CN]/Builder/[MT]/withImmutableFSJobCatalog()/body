{
  try {
    return withJobCatalog(new ImmutableFSJobCatalog(getSysConfig().getConfig(),null,Optional.of(getMetricContext()),isInstrumentationEnabled()));
  }
 catch (  IOException e) {
    throw new RuntimeException("Unable to create FS Job Catalog");
  }
}
