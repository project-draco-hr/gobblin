{
  if (PathUtils.isAncestor(datasetRoot,parent) && !datasetRoot.equals(parent) && this.fs.listStatus(parent).length == 0) {
    this.fs.delete(parent,false);
    deleteEmptyParentDirectories(datasetRoot,parent.getParent());
  }
}
