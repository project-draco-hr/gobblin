{
  updateTaskMetrics();
  for (  Optional<Fork> fork : task.getForks()) {
    if (fork.isPresent()) {
      logger.info(String.format("Queue stats of fork %d of task %s: %s",fork.get().getIndex(),this.task.getTaskId(),fork.get().queueStats().toString()));
    }
  }
}
