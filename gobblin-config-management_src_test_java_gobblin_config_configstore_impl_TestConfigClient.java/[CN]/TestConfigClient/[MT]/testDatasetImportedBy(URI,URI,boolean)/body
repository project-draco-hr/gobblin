{
  Collection<URI> imported=cc.getImportedBy(sourceImports,recursively);
  boolean found=false;
  for (  URI i : imported) {
    if (i.equals(source)) {
      found=true;
    }
  }
  Assert.assertTrue(found);
}
