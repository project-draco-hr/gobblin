{
  if (!(other instanceof Extract)) {
    return false;
  }
  Extract otherExtract=(Extract)other;
  return this.getNamespace().equals(otherExtract.getNamespace()) && this.getTable().equals(otherExtract.getTable()) && this.getExtractId().equals(otherExtract.getExtractId());
}
