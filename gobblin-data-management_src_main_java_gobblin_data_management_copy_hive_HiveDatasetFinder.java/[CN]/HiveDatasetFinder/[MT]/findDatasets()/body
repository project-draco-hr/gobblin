{
  List<HiveDataset> datasets=Lists.newArrayList();
  for (  Table table : getTables(this.db,this.tablePattern)) {
    datasets.add(new HiveDataset(this.fs,this.clientPool,new org.apache.hadoop.hive.ql.metadata.Table(table),this.properties));
  }
  return datasets;
}
