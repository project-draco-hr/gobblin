{
  if (jobProps.containsKey(ConfigurationKeys.JOB_TEMPLATE_PATH)) {
    return ConfigUtils.configToProperties((new ResourceBasedTemplate(jobProps.getProperty(ConfigurationKeys.JOB_TEMPLATE_PATH))).getResolvedConfig(ConfigFactory.parseProperties(jobProps)));
  }
 else {
    return jobProps;
  }
}
