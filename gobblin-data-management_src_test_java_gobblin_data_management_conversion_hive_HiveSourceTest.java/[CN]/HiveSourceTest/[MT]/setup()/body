{
  this.localMetastoreClient=HiveMetastoreClientPool.get(new Properties(),Optional.<String>absent()).getClient().get();
  this.hiveSource=new HiveSource();
  this.updateProvider=MockUpdateProvider.getInstance();
}
