{
  if (args.length != 6) {
    usage();
    System.exit(1);
  }
  InfluxDBReporterExample influxDBReporterExample=new InfluxDBReporterExample(Integer.parseInt(args[0]),Long.parseLong(args[1]),args[2],args[3],args[4],args[5]);
  influxDBReporterExample.run();
}
