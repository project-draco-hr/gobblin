{
  if (!(object instanceof TaskState)) {
    return false;
  }
  TaskState other=(TaskState)object;
  return super.equals(other) && this.jobId.equals(other.jobId) && this.taskId.equals(other.taskId)&& this.startTime == other.startTime && this.endTime == other.endTime && this.duration == other.duration;
}
