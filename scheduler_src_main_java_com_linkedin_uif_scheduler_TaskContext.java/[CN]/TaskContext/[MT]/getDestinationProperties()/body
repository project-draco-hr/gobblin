{
  Properties properties=new Properties();
  for (  String name : this.workUnit.getPropertyNames()) {
    if (name.toLowerCase().startsWith(ConfigurationKeys.WRITER_PREFIX)) {
      properties.setProperty(name,this.workUnit.getProp(name));
    }
  }
  return properties;
}
